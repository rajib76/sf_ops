[dev]
userid = xxxx
password = xxxxxxx
role =xxxxx
account = xxxxx
warehouse = xxxxxx
database = xxxxx
schema = xxxx
[connections]
accountname = xxxx         # Account identifier to connect to Snowflake.
username = xxxx            # User name in the account. Optional.
password = xxxx            # User password. Optional.
dbname = TEST_DB           # Default database. Optional.
schemaname = TEST_SCHEMA   # Default schema. Optional.
warehousename = compute_wh # Default warehouse. Optional.
rolename = sysadmin        # Default role. Optional.
authenticator = snowflake  # Authenticator: 'snowflake', 'externalbrowser' (to use any IdP and a web browser),  https://<okta_account_name>.okta.com (to use Okta natively), 'oauth' to authenticate using OAuth.

